{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
   <div class="container">
      <h1 class="text-center mt-3 mb-3">Vous pouvez donc modifier vos coordonnées</h1>
      {{ form_start(form) }}
         {{ form_widget(form) }}

         <a href="{{ path('update-password') }}" class="btn btn-primary">Modifier le mot de passe</a>

         <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>
      {{ form_end(form) }}
   </div>
{% endblock %}