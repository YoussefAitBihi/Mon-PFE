{% extends 'base.html.twig' %}

{% block title %}Les annonces de : {{ user.fullName }}{% endblock %}

{% block body %}
   <div class="container mt-5">
      <div class="row">
      {% for ad in user.ads %}
         {% include 'ad/_ad.html.twig' %}
      {% endfor %}
      </div>
   </div>
{% endblock %}